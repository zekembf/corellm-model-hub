[project]
name = "corellm-model-hub"
version = "1.0.0"
requires-python = ">=3.12"

dependencies = [
    "fastapi",
    "groq",
    "python-dotenv",
    "pydantic"
]

[tool.vercel]
entrypoint = "api/index.py"